{
  "name": "Hiveborn",
  "install": "npm ci",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npm run dev",
      "description": "Next.js dev server (http://localhost:3000). Storefront and cart work without secrets; Stripe checkout and the Resend webhook require STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, and RESEND_API_KEY."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
